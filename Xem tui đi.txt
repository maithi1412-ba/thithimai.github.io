<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Đừng giận tui nữa</title>
  <style>
    :root{--bg:#0f172a;--card:#0b1220;--accent:#ff416c}
    html,body{height:100%;margin:0;font-family:Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial}
    body{background: radial-gradient(ellipse at 10% 10%, rgba(255,65,108,0.06), transparent 10%), linear-gradient(180deg,#071027 0%, #071022 60%);display:flex;align-items:center;justify-content:center;color:#fff}
    .card{width:min(900px,92vw);background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));border-radius:18px;padding:36px 28px;box-shadow:0 10px 40px rgba(2,6,23,0.7);text-align:center;position:relative;overflow:hidden}
    h1{font-size:clamp(28px,6vw,48px);margin:0 0 12px;line-height:1.02;letter-spacing:0.6px}
    p.lead{margin:0 0 22px;font-size:18px;opacity:0.9}
    .heart-btn{appearance:none;border:0;background:linear-gradient(90deg,#ff7aa2,#ff416c);padding:12px 18px;border-radius:999px;font-weight:600;cursor:pointer;box-shadow:0 8px 30px rgba(255,65,108,0.12);transform:translateY(0);transition:transform .14s}
    .heart-btn:active{transform:translateY(2px)}
    .small{font-size:14px;opacity:0.85}
    .hearts{pointer-events:none;position:absolute;inset:0;overflow:hidden}
    .heart{position:absolute;bottom:-40px;width:28px;height:28px;background:var(--accent);transform:rotate(-20deg);clip-path:path('M10 30 L0 18 C-10 6 8 -6 18 6 C28 -6 46 6 36 18 L26 30 Z');border-radius:6px;opacity:0.95}
    .emoji{font-size:42px;display:inline-block;margin-left:8px}
    .pulse{display:inline-block;animation:pulse 1.2s infinite}
    @keyframes pulse{0%{transform:scale(1)}50%{transform:scale(1.06)}100%{transform:scale(1)}}
    .confetti-piece{position:absolute;width:8px;height:12px;opacity:0.95}
    @media (max-width:420px){.card{padding:22px}}
  </style>
</head>
<body>
  <div class="card" role="main">
    <div class="hearts" aria-hidden="true"></div>
    <h1>Đừng giận tui nữa, <span class="pulse">chơi zới tui đi</span> <span class="emoji">❤️</span></h1>
    <p class="lead">Mỗi giây không chơi zới bà là một giây buồn nè 🥲<br>Click nút bên dưới để tui xin lỗi bằng trái tim tung bay nhé!</p>

    <div>
      <button class="heart-btn" id="sendHeart">Trái tim nè</button>
      <button class="heart-btn" id="saySorry" style="margin-left:10px;background:linear-gradient(90deg,#ffd166,#ff8c42)">Gửi lời yêu thương</button>
    </div>
  </div>

<script>
const heartsLayer = document.querySelector('.hearts');
function createHeart(x){
  const d = document.createElement('div');
  d.className = 'heart';
  const size = Math.random()*18 + 18;
  d.style.width = size+'px';
  d.style.height = size+'px';
  d.style.left = (x || (Math.random()*100)) + '%';
  d.style.background = `radial-gradient(circle at 30% 30%, #fff8, ${['#ff6b6b','#ff416c','#ff7aa2'][Math.floor(Math.random()*3)]} 30%)`;
  const anim = d.animate([
    {transform: `translateY(0) scale(1) rotate(${Math.random()*40-20}deg)`, opacity:1},
    {transform: `translateY(-420px) scale(1.1) rotate(${Math.random()*200-100}deg)`, opacity:0}
  ],{duration: 2200 + Math.random()*900, easing:'cubic-bezier(.12,.8,.32,1)'});
  anim.onfinish = ()=> d.remove();
  heartsLayer.appendChild(d);
}

function burstConfetti(){
  for(let i=0;i<22;i++){
    const c = document.createElement('div');
    c.className = 'confetti-piece';
    const w = Math.random()*10 + 6;
    c.style.width = w+'px';
    c.style.height = (w*0.7)+'px';
    c.style.left = (50 + (Math.random()*240-120)) + 'px';
    c.style.top = '60%';
    c.style.background = ['#ffd166','#ff6b6b','#9be7ff','#caffbf','#ffb3ff'][Math.floor(Math.random()*5)];
    document.body.appendChild(c);
    const a = c.animate([
      {transform: `translateY(0) rotate(0deg)`, opacity:1},
      {transform: `translateY(-600px) rotate(${Math.random()*720-360}deg)`, opacity:0}
    ],{duration:1200+Math.random()*900, easing:'cubic-bezier(.2,.8,.2,1)'});
    a.onfinish = ()=> c.remove();
  }
}

document.getElementById('sendHeart').addEventListener('click', function(e){
  // tạo thật nhiều trái tim, tăng số lượng và tốc độ tung
  for(let i=0;i<80;i++) setTimeout(()=>createHeart(), i*30);
});

const saySorryBtn = document.getElementById('saySorry');
saySorryBtn.addEventListener('click', function(){
  const original = document.querySelector('h1');
  original.innerHTML = 'Tui xin lỗi...<br><small style="opacity:.85">Mãi một tình iu, mãi 1 Thùy Dương💕</small>';
  burstConfetti();
  for(let i=0;i<40;i++) setTimeout(()=>createHeart(), i*40);
  saySorryBtn.animate([{transform:'scale(1)'},{transform:'scale(1.05)'},{transform:'scale(1)'}],{duration:520,iterations:1});
});

window.addEventListener('load', ()=>{
  document.getElementById('sendHeart').focus();
});

window.addEventListener('keydown', (e)=>{ if(e.key.toLowerCase()==='h'){ for(let i=0;i<30;i++) setTimeout(()=>createHeart(), i*40) } });
</script>
</body>
</html>
